🧪 testing: Add tests for normalizeSimilarityTokens

🎯 What:
Added comprehensive unit tests for the `normalizeSimilarityTokens` function in `client/src/lib/productSimilarity.ts` to ensure its string manipulation logic is correct and robust against edge cases.

📊 Coverage:
The new test suite covers the following scenarios:
- Empty or whitespace-only strings
- Diacritics removal (e.g., "Ação" -> "ACAO")
- Punctuation and special characters replacement
- Conversion to uppercase
- Handling of multiple consecutive spaces
- Filtering out tokens with a length of 1
- Proper handling of numbers with a length > 1

✨ Result:
Improved test coverage for a critical string normalization utility, ensuring that future refactoring or logic changes won't introduce silent regressions.
